{
  "name": "qa-wolf-take-home",
  "version": "1.0.0",
  "description": "Playwright + TypeScript suite validating that the first 100 Hacker News /newest articles are sorted newest to oldest, at the UI and API layers",
  "author": "Jimmy Myers",
  "license": "ISC",
  "scripts": {
    "test": "playwright test --project=chromium",
    "test:ui": "playwright test --project=chromium -g \"sorted from newest to oldest\"",
    "test:api": "playwright test --project=chromium -g \"HN API returns newest stories\"",
    "demo:fail": "playwright test --project=demo-fail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
